{% extends '::layout.html.twig' %}

{% block title 'Modificar datos personales' %}
{% block content %}
    <div class="container">
        <ol class="breadcrumb">
            <li><a href="{{ path('portada') }}"><i class="fa fa-home fa-2x"></i></a></li>
            <li class="active">Modificar datos personales</li>
        </ol>
        {{ form(formulario) }}
        <a href="{{ path('portada') }}" class="btn btn-info">Volver a la portada sin guardar los cambios</a>
    </div>
{% endblock %}
